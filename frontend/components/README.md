# Shared UI Components

Global styling layout widgets (like design system cards, grids, dialog wrappers, tooltips, loading spinners, and navigation bars).
These elements are feature-agnostic and support standard styling parameters.
