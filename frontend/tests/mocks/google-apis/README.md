# Google API Test Mocks

Mocks for running frontend unit/integration tests offline:
- Google Maps JavaScript SDK (map initialization, markers, overlays, routes)
- Google Pay SDK (sandbox credentials, merchant verification logic)
- Google OAuth (login response signatures)
