# Frontend Integration Tests

This directory is designated for checking components interacting together (e.g. form validators, dashboard navigation views, and active states).
- Mocks API endpoints and stores.
