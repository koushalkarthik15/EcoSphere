# Next.js App Router Root

Contains the route pages and layout structures:
- `layout.tsx`: Configures standard page HTML elements, styles, Google Fonts injections, and provider wrappers (React Query, Google Identity context).
- `page.tsx`: Single page entrance containing the tabs or dashboards toggle layout.
