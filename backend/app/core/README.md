# App Core Layer

Central app configurations, settings validators, security schemes, database connection pools (if needed), and logging initializers.
- Enforces JWT parsing via OAuth 2.0.
- Implements sanitizers against SQLi and RCE.
